/*
    {
        notes: [],
        active: null,
        active: {
            id: 'asdada',
            title: 'asdada',
            body: 'asdasda',
            imageUrl: 'asda',
            date: 1234
        }
    }
*/

const initialState = {
    notes: [],
    active: null,
}

export const notesReducer = ( state = initialState, action ) => {

    const hola = [];
    let p = {};
    hola.map( )
    switch ( action.type ) {
    
        default:
            return state;
    }
}
